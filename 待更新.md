# LAN File Transfer 待优化记录

记录时间：2026-04-27（状态更新：2026-08-16）

## 状态总览

- ✅ 已修复：第 2、3、4、5、6、7 项（见各条下方说明）
- 🔶 第 1 项（LAN IP 选择）此前"手动选择"接口存在但未生效，现已修复
- 🔶 新发现并已修复的安全问题：`/peer/upload` 零认证推文件、配对自动接受可劫持、WebSocket 跨站连接、跨站写请求防护

## 问题汇总（按优先级）

### 高优先级

#### 1. LAN IP 选择错误（多网卡/VPN 下不稳定）— ✅ 已修复（2026-08-16）

- 位置：`app.py`（`start_server` / `get_lan_ip` / `get_selected_lan_ip`）
- 问题（历史）：`get_lan_ip()` 通过连接外网 DNS 推断本机 IP，多网卡/VPN 时可能选错网卡
- 修复：
  - `start_server` 现在优先使用用户选择并持久化的 `selected_lan_ip`（此前接口有保存但启动路径未消费，功能失效）
  - 前端设置面板新增"局域网 IP"下拉选择（`/settings/lan-ip`），保存后重启生效
  - 用户未选择时仍自动回退候选列表第一个可用 IP

#### 2. UDP 广播发现单向（某些网络环境不可见）— ✅ 已修复（2026-04-27）

- 位置：`app.py` `run_peer_discovery`
- 现状：已支持子网定向广播 + mDNS/Zeroconf 双通道 + 启动 TCP 邻居扫描（±15 IP）+ 对已配对设备周期 TCP 探活
- 说明：探活已移入独立线程（`lft-tcp-probe`），不再阻塞 UDP 广播循环；邻居扫描改为单次快速探测（0.6s/1.0s 超时），避免丢包网络下拖慢启动

### 中高优先级

#### 3. 健康检查超时过短 — ✅ 已修复（2026-04-27）

- 位置：`app.py` `check_peer_health`
- 现状：connect/read 超时 1.5/3.0s + 2 次尝试 + 0.2-0.8s 随机抖动；`check_peer_health` 支持按调用方指定尝试次数与超时

#### 4. 中继重试流复位失败 — ✅ 已修复（2026-04-27）

- 位置：`app.py` `relay_file_to_paired_desktop`
- 现状：重试前 `seek(0)` + `tell()` 位置校验 + 文件大小比对，任何校验失败直接返回错误，不再发送空流/截断流

### 中优先级

#### 5. 临时文件清理路径不一致 — ✅ 已修复（2026-08-16，含根因）

- 位置：`app.py` `cleanup_stale_transient_files` / `cleanup_transient_record_file` / `attach_response_close_hooks`
- 根因（本次查明）：`send_file` 返回的响应是 `direct_passthrough` 模式，WSGI 服务器不会调用 `Response.close()`，因此挂在 `call_on_close` 上的"下载后清理瞬态文件/标记已下载"回调**从未执行过**——这才是 `transient_uploads` 长期堆积的真正原因（此前误判为"路径不一致"）。
- 修复：`attach_response_close_hooks` 手动将响应体包装为 `ClosingIterator`，保证响应结束（含客户端中断）时触发回调；启动时仍保留 >24h 残留清理兜底。
- 补充（2026-08-16）：修复 `/upload` 瞬态路径死代码——唯一前缀名（时间戳+ID+原名）此前被覆盖未生效，现瞬态文件按唯一前缀落盘

#### 6. 同名文件并发竞争 — ✅ 已修复（2026-04-27）

- 位置：`app.py` `allocate_unique_file_path`（`reserve=True` + `file_lock`）
- 现状：3 处文件分配路径均原子化预留，避免并发同名覆盖
- 补充（2026-08-16）：内存记录缓存增加上限（1500 条），超出自动淘汰最旧（历史以数据库为准，下载/保存接口支持 DB 回退），避免长运行内存无界增长

### 低优先级

#### 7. 日志能力不足 — ✅ 基本修复（2026-04-27）

- 现状：配对/发现/中继/健康检查/持久化关键路径已有结构化日志

## 新增修复（2026-08-16，安全问题）

1. **`/peer/upload` 来源校验**：接收端校验 `remote_addr` 必须与配对记录/最近发现记录一致，拒绝伪造 `X-Peer-Device-Id` 的推文件；移除"按设备名匹配"的配对条目迁移（设备名可伪造）。
2. **配对 auto-accept 防劫持**：`/pairing/request` 仅当请求来源 IP 与配对记录/发现记录一致时自动接受，否则转入人工确认流程。
3. **跨站防护**：新增 Origin 白名单——所有写请求（POST/PUT/PATCH/DELETE）与 WebSocket 升级请求均在握手前校验 Origin/Sec-WebSocket-Origin（恶意 Origin 直接 403），拦截恶意网页通过浏览器对本机服务的 CSRF/CSWSH；校验含同源兜底（Origin 与 Host 一致即放行），正常访问（任意 IP/端口/转发场景）零误伤。
4. **SQLite WAL**：历史库启用 WAL + busy_timeout，降低并发"database is locked"概率。
5. **settings.json 读写加锁**：读-改-写原子化，避免并发持久化互相覆盖。
6. **下载状态标记延迟**：`/files/<id>` 改为响应完成（call_on_close）后才标记"已下载"。
7. **内存缓存上限**：`records`/`record_map` 上限 1500 条，超出自动淘汰最旧；下载/保存接口支持从历史数据库回退，长运行不再无界增长。
8. **发现线程解耦**：TCP 探活移入独立线程（`lft-tcp-probe`），UDP 广播循环不再被健康检查阻塞；启动邻居扫描改为单次快速探测（0.6s/1.0s）。
9. **修复手机扫码无法进入（v1.9.3）**：`get_lan_ipv4_candidates()` 字符串排序把 Hyper-V/WSL 虚拟网卡（172.x）排在真实局域网 IP（192.168.x）前面，导致二维码指向手机不可达的地址；现改为默认路由出口（UDP connect 成功）优先。
