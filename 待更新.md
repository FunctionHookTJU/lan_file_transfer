# LAN File Transfer 待优化记录

记录时间：2026-04-27

## 问题汇总（按优先级）

### 高优先级

#### 1. LAN IP 选择错误（多网卡/VPN 下不稳定）

- 位置：`app.py:55-100`
- 问题：`get_lan_ip()` 通过连接外网 DNS 推断本机 IP，多网卡/VPN 时可能选错网卡
- 影响：配对回调地址错误，导致双向传输不对称

- 优化方案：
  - 支持多网卡候选列表，用户可手动指定/选择
  - 或从已配对设备的成功连接反向推导正确 IP

#### 2. UDP 广播发现单向（某些网络环境不可见）

- 位置：`app.py:1275-1348`
- 问题：仅使用 UDP 广播 `255.255.255.255`，某些路由器丢弃全局广播
- 影响：A 能发现 B，但 B 看不到 A

- 优化方案：
  - 增加 TCP 主动探测已配对设备
  - 发现端口可配置

### 中高优先级

#### 3. 健康检查超时过短

- 位置：`app.py:803-813`
- 问题：connect/read timeout 仅 0.35/0.6 秒，轻微网络抖动误判离线
- 影响：正常网络下误认为对端离线，触发不必要的重试

- 优化方案：
  - 改为 1.5s + 2 次重试
  - 添加抖动容忍

#### 4. 中继重试流复位失败

- 位置：`app.py:945-949`
- 问题：`seek(0)` 失败被静默忽略，继续发送空流/截断流
- 影响：重试时发送不完整文件

- 优化方案：
  - 文件流必须 seek 成功才允许重试，否则放弃
  - 重试前检查文件大小

### 中优先级

#### 5. 临时文件清理路径不一致

- 位置：多处
- 问题：某些下载路径不会触发清理，`transient_uploads` 长期堆积
- 影响：磁盘空间浪费，长期运行可能影响性能

- 优化方案：
  - 所有上传/下载流程统一经过 transient_dir
  - 定期清理 (>24h) 或启动时清理

#### 6. 同名文件并发竞争

- 位置：`app.py:288-313`
- 问题：`allocate_unique_file_path()` 非原子检查
- 影响：多个并发上传同名文件时可能冲突覆盖

- 优化方案：
  - 为同名写入增加原子化保护（或加锁）

### 低优先级

#### 7. 日志能力不足

- 位置：多处静默 `pass`
- 问题：排障信息不够，关键流程缺少日志

- 优化方案：
  - 为配对/发现/中继关键路径补充结构化日志